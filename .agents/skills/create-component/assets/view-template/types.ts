// View-level route params are defined in src/navigation/types.ts
// and passed via useRoute() hook, not as component props.
// Only add a types.ts here if this view exposes a reusable sub-component with props.
